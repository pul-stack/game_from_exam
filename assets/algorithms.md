| Категория | Алгоритмы |
|-----------|-----------|
|**Лёгкие** | 1. Спавн врагов с весами вероятностей (`spawn_enemy`)<br>2. Проверка занятости ячейки (`is_cell_occupied`)<br>3. Движение снаряда по вектору (`Missile.update`)<br>4. Конечный автомат состояний игры (`game_state`) |
|**Средние** | 1. Приоритетный поиск цели — First-Target Priority (`find_target`)<br>2. Самонаводящийся снаряд — Homing Missile (`Missile.update`)<br>3. Система случайных способностей врагов (`Abilities.get_ability`) |
|**Сложные**  | 1. Двойной выстрел с поиском второй цели (`find_second_target`)<br>2. Преобразование экранных координат в индексы сетки — Hover Detection (`update_hover`)<br>3. Многоступенчатая система сложности с масштабированием (`difficulty_settings` + `spawn_enemy`) |
