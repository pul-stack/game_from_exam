| Категория | Алгоритмы |
|-----------|-----------|
|**Лёгкие** | 1. Спавн врагов с весами вероятностей (`spawn_enemy`)<br>2. Проверка занятости ячейки (`is_cell_occupied`)<br>3. Движение снаряда по вектору (`Missile.update`)<br>4. Конечный список состояний игры (`game_state`) |
|**Средние** | 1. Приоритетный поиск цели (`find_target`)<br>2. Самонаводящийся снаряд  (`Missile.update`)<br>3. Случайные способности врагов (`Abilities.get_ability`) |
|**Сложные**  | 1. Двойной выстрел с поиском второй цели (`find_second_target`)<br>2. Преобразование экранных координат в индексы сетки (`update_hover`)<br>3. Многоступенчатая система сложности (`difficulty_settings` + `spawn_enemy`) |
